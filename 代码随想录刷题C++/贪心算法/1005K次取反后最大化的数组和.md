# 题目链接

https://leetcode.cn/problems/maximize-sum-of-array-after-k-negations/description/

## C++

```cpp
class Solution {
    static bool cmp(int a, int b) { 
        return abs(a) > abs(b); 
    }

public:
    int largestSumAfterKNegations(vector<int>& nums, int k) {
        sort(nums.begin(), nums.end(), cmp);   // 按照绝对值降序排序
        for (int i = 0; i < nums.size(); i++) {
            if (nums[i] < 0 && k > 0) {
                nums[i] *= -1;
                k--;
            }
        }
        if (k % 2 != 0) {   // 奇数，最后一个数取反，偶数的话不用变
            nums[nums.size() - 1] *= -1;
        }

        int ans = 0;
        for (int i = 0; i < nums.size(); i++) {
            ans += nums[i];
        }
        return ans;
    }
};
```